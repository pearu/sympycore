"""
This file is generated by the sympycore/basealgebra/mk_pairs.py script.
DO NOT CHANGE THIS FILE DIRECTLY!!!
"""

from ..utils import NUMBER, SYMBOL, TERMS, FACTORS, RedirectOperation
from ..arithmetic.numbers import ExtendedNumber, normalized_fraction, FractionTuple

def div(a, b, inttypes = (int, long)):
    if isinstance(b, inttypes):
        if isinstance(a, inttypes):
            if not b:
                if not a:
                    return ExtendedNumber.get_undefined()
                return ExtendedNumber.get_zoo()
            return normalized_fraction(a, b)
        if not b:
            if isinstance(a, ExtendedNumber):
                return a / b
            return ExtendedNumber.get_zoo()
        if b == 1:
            return a
        return FractionTuple((1,b)) * a
    return a / b




def neg_method(self, NUMBER=NUMBER, TERMS=TERMS, new=object.__new__):
    cls = self.__class__
    lhead = self.head
    if lhead is NUMBER:
        #NEG_NUMBER(OP=self)
        #RETURN_NEW(HEAD=NUMBER; DATA=-self.data)
        #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=-self.data)
        obj = new(cls)
        obj.head = NUMBER
        obj.data = -self.data
        return obj
    elif lhead is TERMS:
        #NEG_TERMS(OP=self)
        op_pairs = self.data
        if len(op_pairs)==1:
            t, c = op_pairs.items()[0]
            c = -c
            if c==1:
                return t
            #RETURN_NEW(HEAD=TERMS; DATA={t:c})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t:c})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {t:c}
            return obj
        #NEG_DICT_VALUES(DICT_IN=self.data; DICT_OUT=pairs)
        pairs = dict([(t, -c) for t,c in self.data.iteritems()])
        #RETURN_NEW(HEAD=TERMS; DATA=pairs)
        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
        obj = new(cls)
        obj.head = TERMS
        obj.data = pairs
        return obj
    else:
        #NEG_SYMBOL(OP=self)
        #RETURN_NEW(HEAD=TERMS; DATA={self: -1})
        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: -1})
        obj = new(cls)
        obj.head = TERMS
        obj.data = {self: -1}
        return obj

def rsub_method(self, other, NUMBER=NUMBER, TERMS=TERMS, FACTORS=FACTORS, new=object.__new__):
    cls = self.__class__
    lhead = self.head
    if isinstance(other, cls.coefftypes):
        if lhead is NUMBER:
            #SUB_VALUE_NUMBER(VALUE=other; RHS=self)
            #RETURN_NEW(HEAD=NUMBER; DATA=other - self.data)
            #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=other - self.data)
            obj = new(cls)
            obj.head = NUMBER
            obj.data = other - self.data
            return obj
        elif lhead is TERMS:
            #SUB_VALUE_TERMS(VALUE=other; RHS=self)
            _tmp17 = other
            try:
                if not _tmp17:
                    #NEG_TERMS(OP=self)
                    op_pairs = self.data
                    if len(op_pairs)==1:
                        t, c = op_pairs.items()[0]
                        c = -c
                        if c==1:
                            return t
                        #RETURN_NEW(HEAD=TERMS; DATA={t:c})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t:c})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {t:c}
                        return obj
                    #NEG_DICT_VALUES(DICT_IN=self.data; DICT_OUT=pairs)
                    pairs = dict([(t, -c) for t,c in self.data.iteritems()])
                    #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = pairs
                    return obj
            except RedirectOperation:
                pass
            #NEG_DICT_VALUES(DICT_IN=self.data; DICT_OUT=pairs)
            pairs = dict([(t, -c) for t,c in self.data.iteritems()])
            one = cls.one
            #ADD_VALUE_DICT(DICT=pairs; VALUE=_tmp17)
            #ADD_TERM_VALUE_DICT(TERM=one; VALUE=_tmp17; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(one)
            if b is None:
                pairs[one] = _tmp17
            else:
                c = b + _tmp17
                try:
                    if c:
                        pairs[one] = c
                    else:
                        del pairs[one]
                except RedirectOperation:
                    pairs[one] = c
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        else:
            #SUB_VALUE_SYMBOL(VALUE=other; RHS=self)
            _tmp29 = other
            try:
                if not _tmp29:
                    #RETURN_NEW(HEAD=TERMS; DATA={self: -1})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: -1})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {self: -1}
                    return obj
            except RedirectOperation:
                pass
            #RETURN_NEW(HEAD=TERMS; DATA={cls.one: _tmp29, self: -1})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={cls.one: _tmp29, self: -1})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {cls.one: _tmp29, self: -1}
            return obj
    other = cls.convert(other, False)
    if other is NotImplemented:
        return other
    return other - self

def rdiv_method(self, other, NUMBER=NUMBER, TERMS=TERMS, FACTORS=FACTORS, new=object.__new__):
    cls = self.__class__
    lhead = self.head
    if isinstance(other, cls.coefftypes):
        if lhead is NUMBER:
            #DIV_VALUE_NUMBER(VALUE=other; RHS=self)
            #RETURN_NEW(HEAD=NUMBER; DATA=div(other, self.data))
            #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=div(other, self.data))
            obj = new(cls)
            obj.head = NUMBER
            obj.data = div(other, self.data)
            return obj
        elif lhead is TERMS:
            #DIV_VALUE_TERMS(VALUE=other; RHS=self)
            #MUL_ZERO_OP(VALUE=other; OP=self)
            try:
                if not other:
                    if self.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            pairs = self.data
            if len(pairs)==1:
                t, c = pairs.items()[0]
                c = div(other, c)
                t = 1/t
                if c==1:
                    return t
                if t==cls.one:
                    return cls.convert(c)
                #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c}
                return obj
            #NEWINSTANCE(OBJ=_tmp37; HEAD=FACTORS; DATA={self: -1})
            _tmp37 = new(cls)
            _tmp37.head = FACTORS
            _tmp37.data = {self: -1}
            if other==1:
                return _tmp37
            #RETURN_NEW(HEAD=TERMS; DATA={_tmp37: other})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp37: other})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {_tmp37: other}
            return obj
        elif lhead is FACTORS:
            #DIV_VALUE_FACTORS(VALUE=other; RHS=self)
            
            pairs = self.data
            if len(pairs)==1:
                t, c = pairs.items()[0]
                c = -c
                if c==1:
                    return t * other
                new_pairs = {t: c}
            else:
                #NEG_DICT_VALUES(DICT_IN=pairs; DICT_OUT=new_pairs)
                new_pairs = dict([(t, -c) for t,c in pairs.iteritems()])
            #NEWINSTANCE(OBJ=_tmp46; HEAD=FACTORS; DATA=new_pairs)
            _tmp46 = new(cls)
            _tmp46.head = FACTORS
            _tmp46.data = new_pairs
            if other==1:
                return _tmp46
            #RETURN_NEW(HEAD=TERMS; DATA={_tmp46: other})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp46: other})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {_tmp46: other}
            return obj
        else:
            #DIV_VALUE_SYMBOL(VALUE=other; RHS=self)
            _tmp51 = other
            try:
                if not _tmp51:
                    return cls.zero
            except RedirectOperation:
                pass
            #NEWINSTANCE(OBJ=obj2; HEAD=FACTORS; DATA={self: -1})
            obj2 = new(cls)
            obj2.head = FACTORS
            obj2.data = {self: -1}
            if _tmp51==1:
                return obj2
            #RETURN_NEW(HEAD=TERMS; DATA={obj2: _tmp51})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={obj2: _tmp51})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {obj2: _tmp51}
            return obj
    other = cls.convert(other, False)
    if other is NotImplemented:
        return other
    return other / self

def add_method(self, other, NUMBER=NUMBER, TERMS=TERMS, FACTORS=FACTORS, new=object.__new__):
    cls = self.__class__
    lhead = self.head
    if type(other) is not cls:
        if isinstance(other, cls.coefftypes):
            if lhead is NUMBER:
                #ADD_NUMBER_VALUE(VALUE=other; LHS=self)
                #RETURN_NEW(HEAD=NUMBER; DATA=self.data + other)
                #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=self.data + other)
                obj = new(cls)
                obj.head = NUMBER
                obj.data = self.data + other
                return obj
            elif lhead is TERMS:
                #ADD_TERMS_VALUE(VALUE=other; LHS=self)
                #ADD_VALUE_TERMS(VALUE=other; RHS=self)
                _tmp59 = other
                try:
                    if not _tmp59:
                        return self
                except RedirectOperation:
                    pass
                pairs = dict(self.data)
                one = cls.one
                #ADD_VALUE_DICT(DICT=pairs; VALUE=_tmp59)
                #ADD_TERM_VALUE_DICT(TERM=one; VALUE=_tmp59; DICT=pairs; DICT_GET=pairs.get)
                b = pairs.get(one)
                if b is None:
                    pairs[one] = _tmp59
                else:
                    c = b + _tmp59
                    try:
                        if c:
                            pairs[one] = c
                        else:
                            del pairs[one]
                    except RedirectOperation:
                        pairs[one] = c
                #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                obj = new(cls)
                obj.head = TERMS
                obj.data = pairs
                return obj
            else:
                #ADD_SYMBOL_VALUE(VALUE=other; LHS=self)
                #ADD_VALUE_SYMBOL(VALUE=other; RHS=self)
                _tmp65 = other
                try:
                    if not _tmp65:
                        return self
                except RedirectOperation:
                    pass
                #RETURN_NEW(HEAD=TERMS; DATA={cls.one: _tmp65, self: 1})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={cls.one: _tmp65, self: 1})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {cls.one: _tmp65, self: 1}
                return obj
        other = cls.convert(other, False)
        if other is NotImplemented:
            return other
    rhead = other.head
    if lhead is NUMBER:
        if rhead is NUMBER:
            #ADD_NUMBER_NUMBER(LHS=self; RHS=other)
            #ADD_VALUE_NUMBER(VALUE=self.data; RHS=other)
            #RETURN_NEW(HEAD=NUMBER; DATA=self.data + other.data)
            #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=self.data + other.data)
            obj = new(cls)
            obj.head = NUMBER
            obj.data = self.data + other.data
            return obj
        elif rhead is TERMS:
            #ADD_NUMBER_TERMS(LHS=self; RHS=other)
            #ADD_VALUE_TERMS(VALUE=self.data; RHS=other)
            _tmp73 = self.data
            try:
                if not _tmp73:
                    return other
            except RedirectOperation:
                pass
            pairs = dict(other.data)
            one = cls.one
            #ADD_VALUE_DICT(DICT=pairs; VALUE=_tmp73)
            #ADD_TERM_VALUE_DICT(TERM=one; VALUE=_tmp73; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(one)
            if b is None:
                pairs[one] = _tmp73
            else:
                c = b + _tmp73
                try:
                    if c:
                        pairs[one] = c
                    else:
                        del pairs[one]
                except RedirectOperation:
                    pairs[one] = c
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        else:
            #ADD_NUMBER_SYMBOL(LHS=self; RHS=other)
            #ADD_VALUE_SYMBOL(VALUE=self.data; RHS=other)
            _tmp79 = self.data
            try:
                if not _tmp79:
                    return other
            except RedirectOperation:
                pass
            #RETURN_NEW(HEAD=TERMS; DATA={cls.one: _tmp79, other: 1})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={cls.one: _tmp79, other: 1})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {cls.one: _tmp79, other: 1}
            return obj
    elif lhead is TERMS:
        if rhead is NUMBER:
            #ADD_TERMS_NUMBER(LHS=self; RHS=other)
            #ADD_TERMS_VALUE(VALUE=other.data; LHS=self)
            #ADD_VALUE_TERMS(VALUE=other.data; RHS=self)
            _tmp84 = other.data
            try:
                if not _tmp84:
                    return self
            except RedirectOperation:
                pass
            pairs = dict(self.data)
            one = cls.one
            #ADD_VALUE_DICT(DICT=pairs; VALUE=_tmp84)
            #ADD_TERM_VALUE_DICT(TERM=one; VALUE=_tmp84; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(one)
            if b is None:
                pairs[one] = _tmp84
            else:
                c = b + _tmp84
                try:
                    if c:
                        pairs[one] = c
                    else:
                        del pairs[one]
                except RedirectOperation:
                    pairs[one] = c
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        elif rhead is TERMS:
            #ADD_TERMS_TERMS(LHS=self; RHS=other)
            pairs = dict(self.data)
            pairs_get = pairs.get
            for t,c in other.data.iteritems():
                #ADD_TERM_VALUE_DICT(TERM=t; VALUE=c; DICT=pairs; DICT_GET=pairs_get)
                b = pairs_get(t)
                if b is None:
                    pairs[t] = c
                else:
                    c = b + c
                    try:
                        if c:
                            pairs[t] = c
                        else:
                            del pairs[t]
                    except RedirectOperation:
                        pairs[t] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        else:
            #ADD_TERMS_SYMBOL(LHS=self; RHS=other)
            pairs = dict(self.data)
            #ADD_TERM_VALUE_DICT(TERM=other; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(other)
            if b is None:
                pairs[other] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[other] = c
                    else:
                        del pairs[other]
                except RedirectOperation:
                    pairs[other] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
    else:
        if rhead is NUMBER:
            #ADD_SYMBOL_NUMBER(LHS=self; RHS=other)
            #ADD_SYMBOL_VALUE(VALUE=other.data; LHS=self)
            #ADD_VALUE_SYMBOL(VALUE=other.data; RHS=self)
            _tmp101 = other.data
            try:
                if not _tmp101:
                    return self
            except RedirectOperation:
                pass
            #RETURN_NEW(HEAD=TERMS; DATA={cls.one: _tmp101, self: 1})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={cls.one: _tmp101, self: 1})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {cls.one: _tmp101, self: 1}
            return obj
        elif rhead is TERMS:
            #ADD_SYMBOL_TERMS(LHS=self; RHS=other)
            #ADD_TERMS_SYMBOL(LHS=other; RHS=self)
            pairs = dict(other.data)
            #ADD_TERM_VALUE_DICT(TERM=self; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(self)
            if b is None:
                pairs[self] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[self] = c
                    else:
                        del pairs[self]
                except RedirectOperation:
                    pairs[self] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        else:
            #ADD_SYMBOL_SYMBOL(LHS=self; RHS=other)
            if self == other:
                pairs = {self: 2}
            else:
                pairs = {self: 1, other: 1}
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj

def sub_method(self, other, NUMBER=NUMBER, TERMS=TERMS, FACTORS=FACTORS, new=object.__new__):
    cls = self.__class__
    lhead = self.head
    if type(other) is not cls:
        if isinstance(other, cls.coefftypes):
            if lhead is NUMBER:
                #SUB_NUMBER_VALUE(VALUE=other; LHS=self)
                #RETURN_NEW(HEAD=NUMBER; DATA=self.data - other)
                #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=self.data - other)
                obj = new(cls)
                obj.head = NUMBER
                obj.data = self.data - other
                return obj
            elif lhead is TERMS:
                #SUB_TERMS_VALUE(VALUE=other; LHS=self)
                #ADD_VALUE_TERMS(VALUE=-other; RHS=self)
                _tmp117 = -other
                try:
                    if not _tmp117:
                        return self
                except RedirectOperation:
                    pass
                pairs = dict(self.data)
                one = cls.one
                #ADD_VALUE_DICT(DICT=pairs; VALUE=_tmp117)
                #ADD_TERM_VALUE_DICT(TERM=one; VALUE=_tmp117; DICT=pairs; DICT_GET=pairs.get)
                b = pairs.get(one)
                if b is None:
                    pairs[one] = _tmp117
                else:
                    c = b + _tmp117
                    try:
                        if c:
                            pairs[one] = c
                        else:
                            del pairs[one]
                    except RedirectOperation:
                        pairs[one] = c
                #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                obj = new(cls)
                obj.head = TERMS
                obj.data = pairs
                return obj
            else:
                #SUB_SYMBOL_VALUE(VALUE=other; LHS=self)
                #ADD_SYMBOL_VALUE(OBJ=obj; LHS=self; VALUE=-other)
                #ADD_VALUE_SYMBOL(VALUE=-other; RHS=self)
                _tmp124 = -other
                try:
                    if not _tmp124:
                        return self
                except RedirectOperation:
                    pass
                #RETURN_NEW(HEAD=TERMS; DATA={cls.one: _tmp124, self: 1})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={cls.one: _tmp124, self: 1})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {cls.one: _tmp124, self: 1}
                return obj
        other = cls.convert(other, False)
        if other is NotImplemented:
            return other
    rhead = other.head
    if lhead is NUMBER:
        if rhead is NUMBER:
            #SUB_NUMBER_NUMBER(LHS=self; RHS=other)
            #SUB_VALUE_NUMBER(VALUE=self.data; RHS=other)
            #RETURN_NEW(HEAD=NUMBER; DATA=self.data - other.data)
            #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=self.data - other.data)
            obj = new(cls)
            obj.head = NUMBER
            obj.data = self.data - other.data
            return obj
        elif rhead is TERMS:
            #SUB_NUMBER_TERMS(LHS=self; RHS=other)
            #SUB_VALUE_TERMS(VALUE=self.data; RHS=other)
            _tmp132 = self.data
            try:
                if not _tmp132:
                    #NEG_TERMS(OP=other)
                    op_pairs = other.data
                    if len(op_pairs)==1:
                        t, c = op_pairs.items()[0]
                        c = -c
                        if c==1:
                            return t
                        #RETURN_NEW(HEAD=TERMS; DATA={t:c})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t:c})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {t:c}
                        return obj
                    #NEG_DICT_VALUES(DICT_IN=other.data; DICT_OUT=pairs)
                    pairs = dict([(t, -c) for t,c in other.data.iteritems()])
                    #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = pairs
                    return obj
            except RedirectOperation:
                pass
            #NEG_DICT_VALUES(DICT_IN=other.data; DICT_OUT=pairs)
            pairs = dict([(t, -c) for t,c in other.data.iteritems()])
            one = cls.one
            #ADD_VALUE_DICT(DICT=pairs; VALUE=_tmp132)
            #ADD_TERM_VALUE_DICT(TERM=one; VALUE=_tmp132; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(one)
            if b is None:
                pairs[one] = _tmp132
            else:
                c = b + _tmp132
                try:
                    if c:
                        pairs[one] = c
                    else:
                        del pairs[one]
                except RedirectOperation:
                    pairs[one] = c
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        else:
            #SUB_NUMBER_SYMBOL(LHS=self; RHS=other)
            #SUB_VALUE_SYMBOL(VALUE=self.data; RHS=other)
            _tmp145 = self.data
            try:
                if not _tmp145:
                    #RETURN_NEW(HEAD=TERMS; DATA={other: -1})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: -1})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {other: -1}
                    return obj
            except RedirectOperation:
                pass
            #RETURN_NEW(HEAD=TERMS; DATA={cls.one: _tmp145, other: -1})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={cls.one: _tmp145, other: -1})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {cls.one: _tmp145, other: -1}
            return obj
    elif lhead is TERMS:
        if rhead is NUMBER:
            #SUB_TERMS_NUMBER(LHS=self; RHS=other)
            #SUB_TERMS_VALUE(VALUE=other.data; LHS=self)
            #ADD_VALUE_TERMS(VALUE=-other.data; RHS=self)
            _tmp152 = -other.data
            try:
                if not _tmp152:
                    return self
            except RedirectOperation:
                pass
            pairs = dict(self.data)
            one = cls.one
            #ADD_VALUE_DICT(DICT=pairs; VALUE=_tmp152)
            #ADD_TERM_VALUE_DICT(TERM=one; VALUE=_tmp152; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(one)
            if b is None:
                pairs[one] = _tmp152
            else:
                c = b + _tmp152
                try:
                    if c:
                        pairs[one] = c
                    else:
                        del pairs[one]
                except RedirectOperation:
                    pairs[one] = c
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        elif rhead is TERMS:
            #SUB_TERMS_TERMS(LHS=self; RHS=other)
            pairs = dict(self.data)
            pairs_get = pairs.get
            for t,c in other.data.iteritems():
                #ADD_TERM_VALUE_DICT(TERM=t; VALUE=-c; DICT=pairs; DICT_GET=pairs_get)
                b = pairs_get(t)
                if b is None:
                    pairs[t] = -c
                else:
                    c = b + -c
                    try:
                        if c:
                            pairs[t] = c
                        else:
                            del pairs[t]
                    except RedirectOperation:
                        pairs[t] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        else:
            #SUB_TERMS_SYMBOL(LHS=self; RHS=other)
            pairs = dict(self.data)
            #ADD_TERM_VALUE_DICT(TERM=other; VALUE=-1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(other)
            if b is None:
                pairs[other] = -1
            else:
                c = b + -1
                try:
                    if c:
                        pairs[other] = c
                    else:
                        del pairs[other]
                except RedirectOperation:
                    pairs[other] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
    else:
        if rhead is NUMBER:
            #SUB_SYMBOL_NUMBER(LHS=self; RHS=other)
            #SUB_SYMBOL_VALUE(VALUE=other.data; LHS=self)
            #ADD_SYMBOL_VALUE(OBJ=obj; LHS=self; VALUE=-other.data)
            #ADD_VALUE_SYMBOL(VALUE=-other.data; RHS=self)
            _tmp170 = -other.data
            try:
                if not _tmp170:
                    return self
            except RedirectOperation:
                pass
            #RETURN_NEW(HEAD=TERMS; DATA={cls.one: _tmp170, self: 1})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={cls.one: _tmp170, self: 1})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {cls.one: _tmp170, self: 1}
            return obj
        elif rhead is TERMS:
            #SUB_SYMBOL_TERMS(LHS=self; RHS=other)
            #NEG_DICT_VALUES(DICT_IN=other.data; DICT_OUT=pairs)
            pairs = dict([(t, -c) for t,c in other.data.iteritems()])
            #ADD_TERM_VALUE_DICT(TERM=self; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(self)
            if b is None:
                pairs[self] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[self] = c
                    else:
                        del pairs[self]
                except RedirectOperation:
                    pairs[self] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        else:
            #SUB_SYMBOL_SYMBOL(LHS=self; RHS=other)
            if self == other:
                return cls.zero
            #RETURN_NEW(HEAD=TERMS; DATA={self: 1, other: -1})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 1, other: -1})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {self: 1, other: -1}
            return obj

def mul_method(self, other, NUMBER=NUMBER, TERMS=TERMS, FACTORS=FACTORS, new=object.__new__):
    cls = self.__class__
    lhead = self.head
    if type(other) is not cls:
        if isinstance(other, cls.coefftypes):
            if lhead is NUMBER:
                #MUL_NUMBER_VALUE(VALUE=other; LHS=self)
                #RETURN_NEW(HEAD=NUMBER; DATA=self.data * other)
                #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=self.data * other)
                obj = new(cls)
                obj.head = NUMBER
                obj.data = self.data * other
                return obj
            elif lhead is TERMS:
                #MUL_TERMS_VALUE(VALUE=other; LHS=self)
                #MUL_VALUE_TERMS(VALUE=other; RHS=self)
                _tmp186 = other
                pairs = self.data
                #MUL_ZERO_OP(VALUE=_tmp186; OP=self)
                try:
                    if not _tmp186:
                        if self.has_active():
                            #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {self: 0}
                            return obj
                        else:
                            return cls.zero
                except RedirectOperation:
                    pass
                    if len(pairs) > 1:
                        #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp186})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp186})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: _tmp186}
                        return obj
                if len(pairs)==1:
                    t, c = pairs.items()[0]
                    c = _tmp186 * c
                    if c==1:
                        return t
                    #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {t: c}
                    return obj
                if _tmp186==1:
                    return self
                pairs = {}
                for t,c in self.data.iteritems():
                    c = _tmp186 * c
                    try:
                        if c or t.has_active():
                            pairs[t] = c
                    except RedirectOperation:
                        pairs[t] = c
                #CANONIZE_TERMS_DICT(DICT=pairs)
                if not pairs:
                    return cls.zero
                if len(pairs)==1:
                   t, c = pairs.items()[0]
                   if c==1:
                       return t
                   if t==cls.one:
                       return cls.convert(c)
                #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                obj = new(cls)
                obj.head = TERMS
                obj.data = pairs
                return obj
            elif lhead is FACTORS:
                #MUL_FACTORS_VALUE(VALUE=other; LHS=self)
                #MUL_SYMBOL_VALUE(VALUE=other; LHS=self)
                #MUL_VALUE_SYMBOL(VALUE=other; RHS=self)
                _tmp199 = other
                #MUL_ZERO_OP(VALUE=_tmp199; OP=self)
                try:
                    if not _tmp199:
                        if self.has_active():
                            #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {self: 0}
                            return obj
                        else:
                            return cls.zero
                except RedirectOperation:
                    pass
                if _tmp199==1:
                    return self
                #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp199})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp199})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {self: _tmp199}
                return obj
            else:
                #MUL_SYMBOL_VALUE(VALUE=other; LHS=self)
                #MUL_VALUE_SYMBOL(VALUE=other; RHS=self)
                _tmp206 = other
                #MUL_ZERO_OP(VALUE=_tmp206; OP=self)
                try:
                    if not _tmp206:
                        if self.has_active():
                            #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {self: 0}
                            return obj
                        else:
                            return cls.zero
                except RedirectOperation:
                    pass
                if _tmp206==1:
                    return self
                #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp206})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp206})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {self: _tmp206}
                return obj
        other = cls.convert(other, False)
        if other is NotImplemented:
            return other
    rhead = other.head
    if lhead is NUMBER:
        if rhead is NUMBER:
            #MUL_NUMBER_NUMBER(LHS=self; RHS=other)
            #MUL_VALUE_NUMBER(VALUE=self.data; RHS=other)
            #RETURN_NEW(HEAD=NUMBER; DATA=self.data * other.data)
            #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=self.data * other.data)
            obj = new(cls)
            obj.head = NUMBER
            obj.data = self.data * other.data
            return obj
        elif rhead is TERMS:
            #MUL_NUMBER_TERMS(LHS=self; RHS=other)
            #MUL_VALUE_TERMS(VALUE=self.data; RHS=other)
            _tmp217 = self.data
            pairs = other.data
            #MUL_ZERO_OP(VALUE=_tmp217; OP=other)
            try:
                if not _tmp217:
                    if other.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={other: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {other: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
                if len(pairs) > 1:
                    #RETURN_NEW(HEAD=TERMS; DATA={other: _tmp217})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: _tmp217})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {other: _tmp217}
                    return obj
            if len(pairs)==1:
                t, c = pairs.items()[0]
                c = _tmp217 * c
                if c==1:
                    return t
                #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c}
                return obj
            if _tmp217==1:
                return other
            pairs = {}
            for t,c in other.data.iteritems():
                c = _tmp217 * c
                try:
                    if c or t.has_active():
                        pairs[t] = c
                except RedirectOperation:
                    pairs[t] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        elif rhead is FACTORS:
            #MUL_NUMBER_FACTORS(LHS=self; RHS=other)
            #MUL_SYMBOL_VALUE(VALUE=self.data; LHS=other)
            #MUL_VALUE_SYMBOL(VALUE=self.data; RHS=other)
            _tmp230 = self.data
            #MUL_ZERO_OP(VALUE=_tmp230; OP=other)
            try:
                if not _tmp230:
                    if other.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={other: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {other: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            if _tmp230==1:
                return other
            #RETURN_NEW(HEAD=TERMS; DATA={other: _tmp230})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: _tmp230})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {other: _tmp230}
            return obj
        else:
            #MUL_NUMBER_SYMBOL(LHS=self; RHS=other)
            #MUL_VALUE_SYMBOL(VALUE=self.data; RHS=other)
            _tmp237 = self.data
            #MUL_ZERO_OP(VALUE=_tmp237; OP=other)
            try:
                if not _tmp237:
                    if other.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={other: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {other: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            if _tmp237==1:
                return other
            #RETURN_NEW(HEAD=TERMS; DATA={other: _tmp237})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: _tmp237})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {other: _tmp237}
            return obj
    elif lhead is TERMS:
        if rhead is NUMBER:
            #MUL_TERMS_NUMBER(LHS=self; RHS=other)
            #MUL_TERMS_VALUE(VALUE=other.data; LHS=self)
            #MUL_VALUE_TERMS(VALUE=other.data; RHS=self)
            _tmp245 = other.data
            pairs = self.data
            #MUL_ZERO_OP(VALUE=_tmp245; OP=self)
            try:
                if not _tmp245:
                    if self.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
                if len(pairs) > 1:
                    #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp245})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp245})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {self: _tmp245}
                    return obj
            if len(pairs)==1:
                t, c = pairs.items()[0]
                c = _tmp245 * c
                if c==1:
                    return t
                #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c}
                return obj
            if _tmp245==1:
                return self
            pairs = {}
            for t,c in self.data.iteritems():
                c = _tmp245 * c
                try:
                    if c or t.has_active():
                        pairs[t] = c
                except RedirectOperation:
                    pairs[t] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        elif rhead is TERMS:
            #MUL_TERMS_TERMS(LHS=self; RHS=other)
            lpairs = self.data
            rpairs = other.data
            if len(lpairs)==1:
                t1,c1 = lpairs.items()[0]
                if len(rpairs)==1:
                    t2,c2 = rpairs.items()[0]
                    t = t1 * t2
                    c = c1 * c2
                    if t == cls.one:
                        return cls.convert(c)
                    if c==1:
                        return t
                    #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {t: c}
                    return obj
                t = t1 * other
                #RETURN_NEW(HEAD=TERMS; DATA={t: c1})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c1})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c1}
                return obj
            elif len(rpairs)==1:
                t1,c1 = rpairs.items()[0]
                t = t1 * self
                #RETURN_NEW(HEAD=TERMS; DATA={t: c1})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c1})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c1}
                return obj
            if self==other:
                pairs = {self: 2}
            else:
                pairs = {self: 1, other: 1}
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
        elif rhead is FACTORS:
            #MUL_TERMS_FACTORS(LHS=self; RHS=other)
            #MUL_FACTORS_TERMS(LHS=other; RHS=self)
            rpairs = self.data
            if len(rpairs)==1:
                t1,c1 = rpairs.items()[0]
                t = t1 * other
                #RETURN_NEW(HEAD=TERMS; DATA={t: c1})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c1})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c1}
                return obj
            #MUL_FACTORS_SYMBOL(LHS=other; RHS=self)
            pairs = dict(other.data)
            #MUL_DICT_SYMBOL(DICT=pairs; RHS=self)
            #ADD_TERM_VALUE_DICT(TERM=self; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(self)
            if b is None:
                pairs[self] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[self] = c
                    else:
                        del pairs[self]
                except RedirectOperation:
                    pairs[self] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
        else:
            #MUL_TERMS_SYMBOL(LHS=self; RHS=other)
            pairs = self.data
            if len(pairs)==1:
                t,c = pairs.items()[0]
                t = t * other
                if t==cls.one:
                    return cls.convert(c)
                #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c}
                return obj
            #RETURN_NEW(HEAD=FACTORS; DATA={self: 1, other: 1})
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={self: 1, other: 1})
            obj = new(cls)
            obj.head = FACTORS
            obj.data = {self: 1, other: 1}
            return obj
    elif lhead is FACTORS:
        if rhead is NUMBER:
            #MUL_FACTORS_NUMBER(LHS=self; RHS=other)
            #MUL_SYMBOL_VALUE(VALUE=other.data; LHS=self)
            #MUL_VALUE_SYMBOL(VALUE=other.data; RHS=self)
            _tmp282 = other.data
            #MUL_ZERO_OP(VALUE=_tmp282; OP=self)
            try:
                if not _tmp282:
                    if self.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            if _tmp282==1:
                return self
            #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp282})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp282})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {self: _tmp282}
            return obj
        elif rhead is TERMS:
            #MUL_FACTORS_TERMS(LHS=self; RHS=other)
            rpairs = other.data
            if len(rpairs)==1:
                t1,c1 = rpairs.items()[0]
                t = t1 * self
                #RETURN_NEW(HEAD=TERMS; DATA={t: c1})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c1})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c1}
                return obj
            #MUL_FACTORS_SYMBOL(LHS=self; RHS=other)
            pairs = dict(self.data)
            #MUL_DICT_SYMBOL(DICT=pairs; RHS=other)
            #ADD_TERM_VALUE_DICT(TERM=other; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(other)
            if b is None:
                pairs[other] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[other] = c
                    else:
                        del pairs[other]
                except RedirectOperation:
                    pairs[other] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
        elif rhead is FACTORS:
            #MUL_FACTORS_FACTORS(LHS=self; RHS=other)
            pairs = dict(self.data)
            pairs_get = pairs.get
            number = 1
            for t,c in other.data.iteritems():
                #MUL_FACTOR_VALUE_DICT(FACTOR=t; VALUE=c; DICT=pairs; DICT_GET=pairs_get; NUMBER=number)
                b = pairs_get(t)
                if b is None:
                    pairs[t] = c
                else:
                    _tmp298 = b + c
                    try:
                        if _tmp298:
                            if t.head is NUMBER:
                                r = t ** _tmp298
                                if r.head is NUMBER:
                                    number *= r
                                    del pairs[t]
                                else:
                                    pairs[t] = _tmp298
                            else:
                                pairs[t] = _tmp298
                        else:
                            del pairs[t]
                    except RedirectOperation:
                        pairs[t] = _tmp298
            #CANONIZE_FACTORS_DICT(DICT=pairs; NUMBER=number)
            if not pairs:
                if number is 1:
                    return cls.one
                return number
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t * number
               if t==cls.one:
                   return number
            if number is 1:
                #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
                #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
                obj = new(cls)
                obj.head = FACTORS
                obj.data = pairs
                return obj
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj * number
        else:
            #MUL_FACTORS_SYMBOL(LHS=self; RHS=other)
            pairs = dict(self.data)
            #MUL_DICT_SYMBOL(DICT=pairs; RHS=other)
            #ADD_TERM_VALUE_DICT(TERM=other; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(other)
            if b is None:
                pairs[other] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[other] = c
                    else:
                        del pairs[other]
                except RedirectOperation:
                    pairs[other] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
    else:
        if rhead is NUMBER:
            #MUL_SYMBOL_NUMBER(LHS=self; RHS=other)
            #MUL_SYMBOL_VALUE(VALUE=other.data; LHS=self)
            #MUL_VALUE_SYMBOL(VALUE=other.data; RHS=self)
            _tmp311 = other.data
            #MUL_ZERO_OP(VALUE=_tmp311; OP=self)
            try:
                if not _tmp311:
                    if self.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            if _tmp311==1:
                return self
            #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp311})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp311})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {self: _tmp311}
            return obj
        elif rhead is TERMS:
            #MUL_SYMBOL_TERMS(LHS=self; RHS=other)
            #MUL_TERMS_SYMBOL(LHS=other; RHS=self)
            pairs = other.data
            if len(pairs)==1:
                t,c = pairs.items()[0]
                t = t * self
                if t==cls.one:
                    return cls.convert(c)
                #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c}
                return obj
            #RETURN_NEW(HEAD=FACTORS; DATA={other: 1, self: 1})
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={other: 1, self: 1})
            obj = new(cls)
            obj.head = FACTORS
            obj.data = {other: 1, self: 1}
            return obj
        elif rhead is FACTORS:
            #MUL_SYMBOL_FACTORS(LHS=self; RHS=other)
            #MUL_FACTORS_SYMBOL(LHS=other; RHS=self)
            pairs = dict(other.data)
            #MUL_DICT_SYMBOL(DICT=pairs; RHS=self)
            #ADD_TERM_VALUE_DICT(TERM=self; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(self)
            if b is None:
                pairs[self] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[self] = c
                    else:
                        del pairs[self]
                except RedirectOperation:
                    pairs[self] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
        else:
            #MUL_SYMBOL_SYMBOL(LHS=self; RHS=other)
            if self == other:
                pairs = {self: 2}
            else:
                pairs = {self: 1, other: 1}
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj

def div_method(self, other, NUMBER=NUMBER, TERMS=TERMS, FACTORS=FACTORS, new=object.__new__):
    cls = self.__class__
    lhead = self.head
    if type(other) is not cls:
        if isinstance(other, cls.coefftypes):
            if lhead is NUMBER:
                #DIV_NUMBER_VALUE(VALUE=other; LHS=self)
                #RETURN_NEW(HEAD=NUMBER; DATA=div(self.data, other))
                #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=div(self.data, other))
                obj = new(cls)
                obj.head = NUMBER
                obj.data = div(self.data, other)
                return obj
            elif lhead is TERMS:
                #DIV_TERMS_VALUE(VALUE=other; LHS=self)
                #MUL_TERMS_VALUE(LHS=self; VALUE=div(1,other))
                #MUL_VALUE_TERMS(VALUE=div(1,other); RHS=self)
                _tmp338 = div(1,other)
                pairs = self.data
                #MUL_ZERO_OP(VALUE=_tmp338; OP=self)
                try:
                    if not _tmp338:
                        if self.has_active():
                            #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {self: 0}
                            return obj
                        else:
                            return cls.zero
                except RedirectOperation:
                    pass
                    if len(pairs) > 1:
                        #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp338})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp338})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: _tmp338}
                        return obj
                if len(pairs)==1:
                    t, c = pairs.items()[0]
                    c = _tmp338 * c
                    if c==1:
                        return t
                    #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {t: c}
                    return obj
                if _tmp338==1:
                    return self
                pairs = {}
                for t,c in self.data.iteritems():
                    c = _tmp338 * c
                    try:
                        if c or t.has_active():
                            pairs[t] = c
                    except RedirectOperation:
                        pairs[t] = c
                #CANONIZE_TERMS_DICT(DICT=pairs)
                if not pairs:
                    return cls.zero
                if len(pairs)==1:
                   t, c = pairs.items()[0]
                   if c==1:
                       return t
                   if t==cls.one:
                       return cls.convert(c)
                #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                obj = new(cls)
                obj.head = TERMS
                obj.data = pairs
                return obj
            elif lhead is FACTORS:
                #DIV_FACTORS_VALUE(VALUE=other; LHS=self)
                #MUL_FACTORS_VALUE(LHS=self; VALUE=div(1,other))
                #MUL_SYMBOL_VALUE(VALUE=div(1,other); LHS=self)
                #MUL_VALUE_SYMBOL(VALUE=div(1,other); RHS=self)
                _tmp352 = div(1,other)
                #MUL_ZERO_OP(VALUE=_tmp352; OP=self)
                try:
                    if not _tmp352:
                        if self.has_active():
                            #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {self: 0}
                            return obj
                        else:
                            return cls.zero
                except RedirectOperation:
                    pass
                if _tmp352==1:
                    return self
                #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp352})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp352})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {self: _tmp352}
                return obj
            else:
                #DIV_SYMBOL_VALUE(VALUE=other; LHS=self)
                #MUL_VALUE_SYMBOL(VALUE=div(1, other); RHS=self)
                _tmp359 = div(1, other)
                #MUL_ZERO_OP(VALUE=_tmp359; OP=self)
                try:
                    if not _tmp359:
                        if self.has_active():
                            #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {self: 0}
                            return obj
                        else:
                            return cls.zero
                except RedirectOperation:
                    pass
                if _tmp359==1:
                    return self
                #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp359})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp359})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {self: _tmp359}
                return obj
        other = cls.convert(other, False)
        if other is NotImplemented:
            return other
    rhead = other.head
    if lhead is NUMBER:
        if rhead is NUMBER:
            #DIV_NUMBER_NUMBER(LHS=self; RHS=other)
            #DIV_VALUE_NUMBER(VALUE=self.data; RHS=other)
            #RETURN_NEW(HEAD=NUMBER; DATA=div(self.data, other.data))
            #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=div(self.data, other.data))
            obj = new(cls)
            obj.head = NUMBER
            obj.data = div(self.data, other.data)
            return obj
        elif rhead is TERMS:
            #DIV_NUMBER_TERMS(LHS=self; RHS=other)
            #DIV_VALUE_TERMS(VALUE=self.data; RHS=other)
            #MUL_ZERO_OP(VALUE=self.data; OP=other)
            try:
                if not self.data:
                    if other.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={other: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={other: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {other: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            pairs = other.data
            if len(pairs)==1:
                t, c = pairs.items()[0]
                c = div(self.data, c)
                t = 1/t
                if c==1:
                    return t
                if t==cls.one:
                    return cls.convert(c)
                #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c}
                return obj
            #NEWINSTANCE(OBJ=_tmp370; HEAD=FACTORS; DATA={other: -1})
            _tmp370 = new(cls)
            _tmp370.head = FACTORS
            _tmp370.data = {other: -1}
            if self.data==1:
                return _tmp370
            #RETURN_NEW(HEAD=TERMS; DATA={_tmp370: self.data})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp370: self.data})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {_tmp370: self.data}
            return obj
        elif rhead is FACTORS:
            #DIV_NUMBER_FACTORS(LHS=self; RHS=other)
            #DIV_VALUE_FACTORS(VALUE=self.data; RHS=other)
            
            pairs = other.data
            if len(pairs)==1:
                t, c = pairs.items()[0]
                c = -c
                if c==1:
                    return t * self.data
                new_pairs = {t: c}
            else:
                #NEG_DICT_VALUES(DICT_IN=pairs; DICT_OUT=new_pairs)
                new_pairs = dict([(t, -c) for t,c in pairs.iteritems()])
            #NEWINSTANCE(OBJ=_tmp380; HEAD=FACTORS; DATA=new_pairs)
            _tmp380 = new(cls)
            _tmp380.head = FACTORS
            _tmp380.data = new_pairs
            if self.data==1:
                return _tmp380
            #RETURN_NEW(HEAD=TERMS; DATA={_tmp380: self.data})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp380: self.data})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {_tmp380: self.data}
            return obj
        else:
            #DIV_NUMBER_SYMBOL(LHS=self; RHS=other)
            #DIV_VALUE_SYMBOL(VALUE=self.data; RHS=other)
            _tmp386 = self.data
            try:
                if not _tmp386:
                    return cls.zero
            except RedirectOperation:
                pass
            #NEWINSTANCE(OBJ=obj2; HEAD=FACTORS; DATA={other: -1})
            obj2 = new(cls)
            obj2.head = FACTORS
            obj2.data = {other: -1}
            if _tmp386==1:
                return obj2
            #RETURN_NEW(HEAD=TERMS; DATA={obj2: _tmp386})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={obj2: _tmp386})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {obj2: _tmp386}
            return obj
    elif lhead is TERMS:
        if rhead is NUMBER:
            #DIV_TERMS_NUMBER(LHS=self; RHS=other)
            #DIV_TERMS_VALUE(VALUE=other.data; LHS=self)
            #MUL_TERMS_VALUE(LHS=self; VALUE=div(1,other.data))
            #MUL_VALUE_TERMS(VALUE=div(1,other.data); RHS=self)
            _tmp393 = div(1,other.data)
            pairs = self.data
            #MUL_ZERO_OP(VALUE=_tmp393; OP=self)
            try:
                if not _tmp393:
                    if self.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
                if len(pairs) > 1:
                    #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp393})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp393})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {self: _tmp393}
                    return obj
            if len(pairs)==1:
                t, c = pairs.items()[0]
                c = _tmp393 * c
                if c==1:
                    return t
                #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {t: c}
                return obj
            if _tmp393==1:
                return self
            pairs = {}
            for t,c in self.data.iteritems():
                c = _tmp393 * c
                try:
                    if c or t.has_active():
                        pairs[t] = c
                except RedirectOperation:
                    pairs[t] = c
            #CANONIZE_TERMS_DICT(DICT=pairs)
            if not pairs:
                return cls.zero
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return cls.convert(c)
            #RETURN_NEW(HEAD=TERMS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
            obj = new(cls)
            obj.head = TERMS
            obj.data = pairs
            return obj
        elif rhead is TERMS:
            #DIV_TERMS_TERMS(LHS=self; RHS=other)
            
            if self==other:
                return cls.one
            lpairs = self.data
            rpairs = other.data
            if len(lpairs)==1:
                t1, c1 = lpairs.items()[0]
                if len(rpairs)==1:
                    t2, c2 = rpairs.items()[0]
                    c = div(c1, c2)
                    if t2==t1:
                        return cls.convert(c)
                    if c==1:
                        #RETURN_NEW(HEAD=FACTORS; DATA={t1:1, t2:-1})
                        #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={t1:1, t2:-1})
                        obj = new(cls)
                        obj.head = FACTORS
                        obj.data = {t1:1, t2:-1}
                        return obj
                    #NEWINSTANCE(OBJ=_tmp404; HEAD=FACTORS; DATA={t1:1, t2:-1})
                    _tmp404 = new(cls)
                    _tmp404.head = FACTORS
                    _tmp404.data = {t1:1, t2:-1}
                else:
                    #NEWINSTANCE(OBJ=_tmp404; HEAD=FACTORS; DATA={t1:1, other:-1})
                    _tmp404 = new(cls)
                    _tmp404.head = FACTORS
                    _tmp404.data = {t1:1, other:-1}
                #RETURN_NEW(HEAD=TERMS; DATA={_tmp404:c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp404:c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {_tmp404:c}
                return obj
            elif len(rpairs)==1:
                t2, c2 = rpairs.items()[0]
                c = div(1, c2)
                if t2==self:
                    return cls.convert(c)
                _tmp404 = self / t2
                #RETURN_NEW(HEAD=TERMS; DATA={_tmp404:c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp404:c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {_tmp404:c}
                return obj
            #RETURN_NEW(HEAD=FACTORS; DATA={self:1, other:-1})
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={self:1, other:-1})
            obj = new(cls)
            obj.head = FACTORS
            obj.data = {self:1, other:-1}
            return obj
        elif rhead is FACTORS:
            #DIV_TERMS_FACTORS(LHS=self; RHS=other)
            lpairs = self.data
            if len(lpairs)==1:
                t, c = lpairs.items()[0]
                t = t / other
                if t==cls.one:
                    return cls.convert(c)
                head = t.head
                if head is NUMBER:
                    #RETURN_NEW(HEAD=NUMBER; DATA=t.data * c)
                    #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=t.data * c)
                    obj = new(cls)
                    obj.head = NUMBER
                    obj.data = t.data * c
                    return obj
                elif head is TERMS:
                    #MUL_TERMS_VALUE(LHS=t; VALUE=c)
                    #MUL_VALUE_TERMS(VALUE=c; RHS=t)
                    _tmp419 = c
                    pairs = t.data
                    #MUL_ZERO_OP(VALUE=_tmp419; OP=t)
                    try:
                        if not _tmp419:
                            if t.has_active():
                                #RETURN_NEW(HEAD=TERMS; DATA={t: 0})
                                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: 0})
                                obj = new(cls)
                                obj.head = TERMS
                                obj.data = {t: 0}
                                return obj
                            else:
                                return cls.zero
                    except RedirectOperation:
                        pass
                        if len(pairs) > 1:
                            #RETURN_NEW(HEAD=TERMS; DATA={t: _tmp419})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: _tmp419})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {t: _tmp419}
                            return obj
                    if len(pairs)==1:
                        t, c = pairs.items()[0]
                        c = _tmp419 * c
                        if c==1:
                            return t
                        #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {t: c}
                        return obj
                    if _tmp419==1:
                        return t
                    pairs = {}
                    for t,c in t.data.iteritems():
                        c = _tmp419 * c
                        try:
                            if c or t.has_active():
                                pairs[t] = c
                        except RedirectOperation:
                            pairs[t] = c
                    #CANONIZE_TERMS_DICT(DICT=pairs)
                    if not pairs:
                        return cls.zero
                    if len(pairs)==1:
                       t, c = pairs.items()[0]
                       if c==1:
                           return t
                       if t==cls.one:
                           return cls.convert(c)
                    #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = pairs
                    return obj
                else:
                    #MUL_SYMBOL_VALUE(LHS=t; VALUE=c)
                    #MUL_VALUE_SYMBOL(VALUE=c; RHS=t)
                    _tmp431 = c
                    #MUL_ZERO_OP(VALUE=_tmp431; OP=t)
                    try:
                        if not _tmp431:
                            if t.has_active():
                                #RETURN_NEW(HEAD=TERMS; DATA={t: 0})
                                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: 0})
                                obj = new(cls)
                                obj.head = TERMS
                                obj.data = {t: 0}
                                return obj
                            else:
                                return cls.zero
                    except RedirectOperation:
                        pass
                    if _tmp431==1:
                        return t
                    #RETURN_NEW(HEAD=TERMS; DATA={t: _tmp431})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: _tmp431})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {t: _tmp431}
                    return obj
            #DIV_SYMBOL_FACTORS(LHS=self; RHS=other)
            pairs = other.data
            if len(pairs)==1:
                t, c = pairs.items()[0]
                if t==self:
                    c = 1 - c
                    try:
                        if not c:
                            return cls.one
                    except RedirectOperation:
                        pass
                    if c==1:
                        return t
                    else:
                        #RETURN_NEW(HEAD=FACTORS; DATA={t: c})
                        #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={t: c})
                        obj = new(cls)
                        obj.head = FACTORS
                        obj.data = {t: c}
                        return obj
                #RETURN_NEW(HEAD=FACTORS; DATA={t: -c, self: 1})
                #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={t: -c, self: 1})
                obj = new(cls)
                obj.head = FACTORS
                obj.data = {t: -c, self: 1}
                return obj
            #NEG_DICT_VALUES(DICT_IN=other.data; DICT_OUT=pairs)
            pairs = dict([(t, -c) for t,c in other.data.iteritems()])
            #MUL_DICT_SYMBOL(DICT=pairs; RHS=self)
            #ADD_TERM_VALUE_DICT(TERM=self; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(self)
            if b is None:
                pairs[self] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[self] = c
                    else:
                        del pairs[self]
                except RedirectOperation:
                    pairs[self] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
        else:
            #DIV_TERMS_SYMBOL(LHS=self; RHS=other)
            
            pairs = self.data
            if len(pairs)==1:
                t, c = pairs.items()[0]
                if t==other:
                    return cls.convert(c)
                #NEWINSTANCE(OBJ=_tmp448; HEAD=FACTORS; DATA={t:1, other: -1})
                _tmp448 = new(cls)
                _tmp448.head = FACTORS
                _tmp448.data = {t:1, other: -1}
                #RETURN_NEW(HEAD=TERMS; DATA={_tmp448: c})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp448: c})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {_tmp448: c}
                return obj
            #RETURN_NEW(HEAD=FACTORS; DATA={self: 1, other: -1})
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={self: 1, other: -1})
            obj = new(cls)
            obj.head = FACTORS
            obj.data = {self: 1, other: -1}
            return obj
    elif lhead is FACTORS:
        if rhead is NUMBER:
            #DIV_FACTORS_NUMBER(LHS=self; RHS=other)
            #DIV_FACTORS_VALUE(VALUE=other.data; LHS=self)
            #MUL_FACTORS_VALUE(LHS=self; VALUE=div(1,other.data))
            #MUL_SYMBOL_VALUE(VALUE=div(1,other.data); LHS=self)
            #MUL_VALUE_SYMBOL(VALUE=div(1,other.data); RHS=self)
            _tmp458 = div(1,other.data)
            #MUL_ZERO_OP(VALUE=_tmp458; OP=self)
            try:
                if not _tmp458:
                    if self.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            if _tmp458==1:
                return self
            #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp458})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp458})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {self: _tmp458}
            return obj
        elif rhead is TERMS:
            #DIV_FACTORS_TERMS(LHS=self; RHS=other)
            rpairs = other.data
            if len(rpairs)==1:
                t, c = rpairs.items()[0]
                t = self / t
                c = div(1, c)
                if t==cls.one:
                    return cls.convert(c)
                head = t.head
                if head is NUMBER:
                    #RETURN_NEW(HEAD=NUMBER; DATA=t.data * c)
                    #NEWINSTANCE(OBJ=obj; HEAD=NUMBER; DATA=t.data * c)
                    obj = new(cls)
                    obj.head = NUMBER
                    obj.data = t.data * c
                    return obj
                elif head is TERMS:
                    #MUL_TERMS_VALUE(LHS=t; VALUE=c)
                    #MUL_VALUE_TERMS(VALUE=c; RHS=t)
                    _tmp468 = c
                    pairs = t.data
                    #MUL_ZERO_OP(VALUE=_tmp468; OP=t)
                    try:
                        if not _tmp468:
                            if t.has_active():
                                #RETURN_NEW(HEAD=TERMS; DATA={t: 0})
                                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: 0})
                                obj = new(cls)
                                obj.head = TERMS
                                obj.data = {t: 0}
                                return obj
                            else:
                                return cls.zero
                    except RedirectOperation:
                        pass
                        if len(pairs) > 1:
                            #RETURN_NEW(HEAD=TERMS; DATA={t: _tmp468})
                            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: _tmp468})
                            obj = new(cls)
                            obj.head = TERMS
                            obj.data = {t: _tmp468}
                            return obj
                    if len(pairs)==1:
                        t, c = pairs.items()[0]
                        c = _tmp468 * c
                        if c==1:
                            return t
                        #RETURN_NEW(HEAD=TERMS; DATA={t: c})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: c})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {t: c}
                        return obj
                    if _tmp468==1:
                        return t
                    pairs = {}
                    for t,c in t.data.iteritems():
                        c = _tmp468 * c
                        try:
                            if c or t.has_active():
                                pairs[t] = c
                        except RedirectOperation:
                            pairs[t] = c
                    #CANONIZE_TERMS_DICT(DICT=pairs)
                    if not pairs:
                        return cls.zero
                    if len(pairs)==1:
                       t, c = pairs.items()[0]
                       if c==1:
                           return t
                       if t==cls.one:
                           return cls.convert(c)
                    #RETURN_NEW(HEAD=TERMS; DATA=pairs)
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA=pairs)
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = pairs
                    return obj
                else:
                    #MUL_SYMBOL_VALUE(LHS=t; VALUE=c)
                    #MUL_VALUE_SYMBOL(VALUE=c; RHS=t)
                    _tmp480 = c
                    #MUL_ZERO_OP(VALUE=_tmp480; OP=t)
                    try:
                        if not _tmp480:
                            if t.has_active():
                                #RETURN_NEW(HEAD=TERMS; DATA={t: 0})
                                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: 0})
                                obj = new(cls)
                                obj.head = TERMS
                                obj.data = {t: 0}
                                return obj
                            else:
                                return cls.zero
                    except RedirectOperation:
                        pass
                    if _tmp480==1:
                        return t
                    #RETURN_NEW(HEAD=TERMS; DATA={t: _tmp480})
                    #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={t: _tmp480})
                    obj = new(cls)
                    obj.head = TERMS
                    obj.data = {t: _tmp480}
                    return obj
            #DIV_FACTORS_SYMBOL(LHS=self; RHS=other)
            pairs = dict(self.data)
            #DIV_DICT_SYMBOL(RHS=other; DICT=pairs)
            #ADD_TERM_VALUE_DICT(TERM=other; VALUE=-1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(other)
            if b is None:
                pairs[other] = -1
            else:
                c = b + -1
                try:
                    if c:
                        pairs[other] = c
                    else:
                        del pairs[other]
                except RedirectOperation:
                    pairs[other] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
        elif rhead is FACTORS:
            #DIV_FACTORS_FACTORS(LHS=self; RHS=other)
            pairs = dict(self.data)
            pairs_get = pairs.get
            number = 1
            for t,c in other.data.iteritems():
                #MUL_FACTOR_VALUE_DICT(FACTOR=t; VALUE=-c; DICT=pairs; DICT_GET=pairs_get; NUMBER=number)
                b = pairs_get(t)
                if b is None:
                    pairs[t] = -c
                else:
                    _tmp493 = b + -c
                    try:
                        if _tmp493:
                            if t.head is NUMBER:
                                r = t ** _tmp493
                                if r.head is NUMBER:
                                    number *= r
                                    del pairs[t]
                                else:
                                    pairs[t] = _tmp493
                            else:
                                pairs[t] = _tmp493
                        else:
                            del pairs[t]
                    except RedirectOperation:
                        pairs[t] = _tmp493
            #CANONIZE_FACTORS_DICT(DICT=pairs; NUMBER=number)
            if not pairs:
                if number is 1:
                    return cls.one
                return number
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t * number
               if t==cls.one:
                   return number
            if number==1:
                #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
                #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
                obj = new(cls)
                obj.head = FACTORS
                obj.data = pairs
                return obj
            #NEWINSTANCE(OBJ=_tmp492; HEAD=FACTORS; DATA=pairs)
            _tmp492 = new(cls)
            _tmp492.head = FACTORS
            _tmp492.data = pairs
            #RETURN_NEW(HEAD=TERMS; DATA={_tmp492: number})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp492: number})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {_tmp492: number}
            return obj
        else:
            #DIV_FACTORS_SYMBOL(LHS=self; RHS=other)
            pairs = dict(self.data)
            #DIV_DICT_SYMBOL(RHS=other; DICT=pairs)
            #ADD_TERM_VALUE_DICT(TERM=other; VALUE=-1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(other)
            if b is None:
                pairs[other] = -1
            else:
                c = b + -1
                try:
                    if c:
                        pairs[other] = c
                    else:
                        del pairs[other]
                except RedirectOperation:
                    pairs[other] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
    else:
        if rhead is NUMBER:
            #DIV_SYMBOL_NUMBER(LHS=self; RHS=other)
            #DIV_SYMBOL_VALUE(VALUE=other.data; LHS=self)
            #MUL_VALUE_SYMBOL(VALUE=div(1, other.data); RHS=self)
            _tmp508 = div(1, other.data)
            #MUL_ZERO_OP(VALUE=_tmp508; OP=self)
            try:
                if not _tmp508:
                    if self.has_active():
                        #RETURN_NEW(HEAD=TERMS; DATA={self: 0})
                        #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: 0})
                        obj = new(cls)
                        obj.head = TERMS
                        obj.data = {self: 0}
                        return obj
                    else:
                        return cls.zero
            except RedirectOperation:
                pass
            if _tmp508==1:
                return self
            #RETURN_NEW(HEAD=TERMS; DATA={self: _tmp508})
            #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={self: _tmp508})
            obj = new(cls)
            obj.head = TERMS
            obj.data = {self: _tmp508}
            return obj
        elif rhead is TERMS:
            #DIV_SYMBOL_TERMS(LHS=self; RHS=other)
            pairs = other.data
            if len(pairs)==1:
                t,c = pairs.items()[0]
                if self==t:
                    return cls.convert(div(1, c))
                #NEWINSTANCE(OBJ=_tmp514; HEAD=FACTORS; DATA={self:1, t:-1})
                _tmp514 = new(cls)
                _tmp514.head = FACTORS
                _tmp514.data = {self:1, t:-1}
                #RETURN_NEW(HEAD=TERMS; DATA={_tmp514: div(1, c)})
                #NEWINSTANCE(OBJ=obj; HEAD=TERMS; DATA={_tmp514: div(1, c)})
                obj = new(cls)
                obj.head = TERMS
                obj.data = {_tmp514: div(1, c)}
                return obj
            #RETURN_NEW(HEAD=FACTORS; DATA={self:1, other:-1})
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={self:1, other:-1})
            obj = new(cls)
            obj.head = FACTORS
            obj.data = {self:1, other:-1}
            return obj
        elif rhead is FACTORS:
            #DIV_SYMBOL_FACTORS(LHS=self; RHS=other)
            pairs = other.data
            if len(pairs)==1:
                t, c = pairs.items()[0]
                if t==self:
                    c = 1 - c
                    try:
                        if not c:
                            return cls.one
                    except RedirectOperation:
                        pass
                    if c==1:
                        return t
                    else:
                        #RETURN_NEW(HEAD=FACTORS; DATA={t: c})
                        #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={t: c})
                        obj = new(cls)
                        obj.head = FACTORS
                        obj.data = {t: c}
                        return obj
                #RETURN_NEW(HEAD=FACTORS; DATA={t: -c, self: 1})
                #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={t: -c, self: 1})
                obj = new(cls)
                obj.head = FACTORS
                obj.data = {t: -c, self: 1}
                return obj
            #NEG_DICT_VALUES(DICT_IN=other.data; DICT_OUT=pairs)
            pairs = dict([(t, -c) for t,c in other.data.iteritems()])
            #MUL_DICT_SYMBOL(DICT=pairs; RHS=self)
            #ADD_TERM_VALUE_DICT(TERM=self; VALUE=1; DICT=pairs; DICT_GET=pairs.get)
            b = pairs.get(self)
            if b is None:
                pairs[self] = 1
            else:
                c = b + 1
                try:
                    if c:
                        pairs[self] = c
                    else:
                        del pairs[self]
                except RedirectOperation:
                    pairs[self] = c
            #CANONIZE_FACTORS_DICT1(DICT=pairs)
            if not pairs:
                return cls.one
            if len(pairs)==1:
               t, c = pairs.items()[0]
               if c==1:
                   return t
               if t==cls.one:
                   return t
            #RETURN_NEW(HEAD=FACTORS; DATA=pairs)
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA=pairs)
            obj = new(cls)
            obj.head = FACTORS
            obj.data = pairs
            return obj
        else:
            #DIV_SYMBOL_SYMBOL(LHS=self; RHS=other)
            if self == other:
                return cls.one
            #RETURN_NEW(HEAD=FACTORS; DATA={self: 1, other: -1})
            #NEWINSTANCE(OBJ=obj; HEAD=FACTORS; DATA={self: 1, other: -1})
            obj = new(cls)
            obj.head = FACTORS
            obj.data = {self: 1, other: -1}
            return obj
